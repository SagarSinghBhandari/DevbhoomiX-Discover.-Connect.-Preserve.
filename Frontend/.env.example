# FastAPI backend base URL. Used when VITE_USE_MOCK_API=false.
VITE_API_BASE_URL=http://localhost:8000

# true = local mock services (no backend required)
# false = real FastAPI endpoints via apiClient
VITE_USE_MOCK_API=true
